name: Java CI

on:
  push:
    branches: [ "master" ]
    paths:
      - 'Eindopdracht-1-taskservice/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'adopt'
          cache: maven

      - name: Build with Maven
        run: mvn -B install --file pom.xml

      - name: Code Analysis
        run: |
          mvn sonar:sonar \
          -Dsonar.projectKey=eindopdracht-1-tygo-joost \
          -Dsonar.host.url=http://sonar.cloud-native-minor.it/ \
          -Dsonar.login=45d50dd41d616b6d7bcb94387588d9ea2a89c7da

      - name: Build the Docker image
        run: docker build . --file /Eindopdracht-1-taskservice Dockerfile